CREATE TABLE studentsa (
  id int8 PRIMARY KEY AUTO_INCREMENT,
  firstName VARCHAR(255) NOT NULL,
  lastName VARCHAR(255) NOT NULL,
  stipend INT NOT NULL
);



-- data.sql is getting used in the tests or not, I think its not.
-- why do we need an external database when we have this data.sql here
-- changes in the application.properties
-- what is this application.sql
-- get these .sql thing fixed

-- deployment stuffs
--


